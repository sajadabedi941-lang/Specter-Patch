SPECTER standalone camera zoom-out (clean)

Contains EXACTLY one file:
  Data/INI/GameData.ini

From ORIGINAL user GameData.ini only.
Changes only:
  MaxCameraHeight = 1000.0
  EnforceMaxCameraHeight = No

DefaultCameraMaxHeight is NOT present.
Do NOT merge with any other patch.
