; SPECTER PATCH — Port / Oil / Trade income wiring notes
; Values come from CountryBalance.ini Country.*IncomeMult fields.
;
; Supply buildings: applicator injects ModuleTag_PatchResourceIncome
;   DepositAmount = SupplyDepositBase × Country.TradeIncomeMult
;
; Oil (TechOilDerrick is shared civilian): map makers / scripts should use
;   Country.OilIncomeMult when granting capture bonuses. Do not fork the
;   shared derrick Object per faction (breaks MP determinism on maps).
;
; Ports / trade centers (when faction Objects exist):
;   Behavior = AutoDepositUpdate ModuleTag_PatchPortIncome
;     DepositTiming = 12000
;     DepositAmount = <round(20 × Country.PortIncomeMult)>
;   End
;   Behavior = AutoDepositUpdate ModuleTag_PatchTradeIncome
;     DepositTiming = 10000
;     DepositAmount = <round(20 × Country.TradeIncomeMult)>
;   End
;
; apply_country_balance.py currently auto-wires SupplyCenter / SupplyStash.
; Extend that tool when dedicated Port Objects are authored per Side.
